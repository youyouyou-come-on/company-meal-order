# 复制为 deploy-remote.env 后再 source：
#   cp deploy-remote.env.example deploy-remote.env
#   source ./deploy-remote.env
#   ./scripts/deploy-remote.sh

# SSH 目标
export DEPLOY_HOST="your.server.ip"
export DEPLOY_USER="root"
export DEPLOY_PORT="22"
export SSH_PASSWORD=""

# 远程部署目录与服务信息
export DEPLOY_PATH="/opt/company-meal-order"
export APP_USER="mealorder"
export SERVICE_NAME="company-meal-order"
export APP_PORT="3000"

# 运行环境
export NODE_MAJOR="22"
export PNPM_VERSION="10.29.3"

# 首次部署必填；后续不填则沿用远程 .env 里的值
export SESSION_PASSWORD="replace-with-a-random-string-at-least-32-chars"
export LOGIN_PASSWORD="replace-with-shared-login-password"
export LOGIN_LOCK_MAX_FAILED_ATTEMPTS="100"
export LOGIN_LOCK_DURATION_MINUTES="15"
export ADMIN_PASSWORD="replace-with-your-admin-password"

# Nginx / 域名 / HTTPS
export INSTALL_NGINX="1"
export APP_DOMAINS="example.com www.example.com"
export ENABLE_HTTPS="1"
export CERTBOT_EMAIL=""

# HTTPS 时建议 true；留空则脚本会自动根据 ENABLE_HTTPS 推断
export COOKIE_SECURE=""

# 首次部署自动执行种子数据；如果不想要示例数据可以改成 0
export SEED_ON_FIRST_DEPLOY="1"

# 部署前本地质量检查：默认跑 lint + build；如需跳过可设为 1
export SKIP_DEPLOY_CHECKS="0"
# Playwright E2E 耗时更长，默认不跑；重要上线前可设为 1
export RUN_DEPLOY_E2E="0"

# 生产 SQLite 数据库备份：默认部署前备份，并安装每日定时备份
export ENABLE_DB_BACKUP="1"
export DB_BACKUP_RETENTION_DAYS="14"
export DB_BACKUP_CRON="23 2 * * *"

# 钉钉点餐提醒：默认不开启；开启后会安装上午 9:30 / 9:50 和下午 15:00 / 15:20 定时提醒
export ENABLE_DINGTALK_REMINDER="0"
export DINGTALK_CLIENT_ID=""
export DINGTALK_CLIENT_SECRET=""
export DINGTALK_AGENT_ID=""
export DINGTALK_REMINDER_URL="https://meal.zcgc.club"
export DINGTALK_REMINDER_FIRST_CRON="30 9 * * 1-6"
export DINGTALK_REMINDER_SECOND_CRON="50 9 * * 1-6"
export DINGTALK_DINNER_REMINDER_FIRST_CRON="0 15 * * 1-6"
export DINGTALK_DINNER_REMINDER_SECOND_CRON="20 15 * * 1-6"
